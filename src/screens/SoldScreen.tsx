import React from 'react';
import { View } from 'react-native';

export default class SoldScreen extends React.Component<any, any> {
  static navigationOptions = {
    title: 'app.json',
  };

  render() {
    return (
      <View />
    )
  }
}
